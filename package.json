{
  "name": "koa-example",
  "version": "0.0.1",
  "description": "koa-example is a simple HTTP server based on koa that handles a REST architecture for handling users",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@koa/router": "^8.0.8",
    "koa": "^2.11.0",
    "koa-bodyparser": "^4.3.0",
    "koa-logger": "^3.2.1"
  }
}
